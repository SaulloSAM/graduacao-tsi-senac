@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Conta", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { id = "logoutForm" }))
    {
        <nav class="navigation navigation-buttons account-actions user-logged navbar navbar-inverse menuBars">
            <ul class="nav navbar-nav navbar-right">
                <li class="link">
                    @Html.ActionLink(" Olá " + @User.Identity.GetUserName(), "Consultar", "Pedido", routeValues: null, htmlAttributes: new { id = "btn-create-account", @class = "btn btn-type-2", title = "Clique aqui para ver seus pedidos..." })
                </li>
                <li class="link"><button type="submit" class="btn btn-type-1" id="botaoSair">Sair</button></li>
            </ul>
        </nav>
    }
}
else
{
    @*<nav class="navigation navigation-buttons account-actions">
            <ul>
                <li>@Html.ActionLink("Criar conta", "Cadastrar", "Conta", routeValues: null, htmlAttributes: new { id = "btn-create-account", @class = "btn btn-type-2" })</li>
                <li>@Html.ActionLink("Login", "Login", "Conta", routeValues: new { ReturnUrl = ViewBag.ReturnUrl }, htmlAttributes: new { id = "btn-login", @class = "btn btn-type-1" })</li>
            </ul>
        </nav>*@

    <nav class="navbar navbar-inverse menuBars">
        <ul class="nav navbar-nav navbar-right">
            <li class="link">@Html.ActionLink("Criar conta", "Cadastrar", "Conta", routeValues: null, htmlAttributes: new { id = "btn-create-account", @class = "btn btn-type-2" })</li>
            <li class="link">@Html.ActionLink("Login", "Login", "Conta", routeValues: new { ReturnUrl = ViewBag.ReturnUrl }, htmlAttributes: new { id = "btn-login", @class = "btn btn-type-1" })</li>
        </ul>
    </nav>
}
